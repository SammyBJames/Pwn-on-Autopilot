import requests

headers = {
    'User-Agent': 'Pwn-on-Autopilot/1.0',
    'X-Custom-Header': 'Hello from Python'
}

data = {
    'username': 'admin',
    'password': 'password123'
}

# TODO: Make a POST request to https://httpbin.org/post with `data` as form data

# TODO: Add the custom headers to the request

# TODO: Send the data as JSON rather than form data
